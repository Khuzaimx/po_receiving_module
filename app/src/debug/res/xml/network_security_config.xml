<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: allows plain HTTP to the loopback address so the app can talk
     to a local mock backend reached via `adb reverse`. Never merged into release. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
